# 将 S3 直连灯带视为可选灯光节点

> 场景发布语义已由 [ADR 0003](0003-scenes-publish-per-node-desired-state.md) 深化：S3 与 C3
> 都是独立寻址目标，不再存在产品层面的“跟随/独立”二选一。

Flux Hub 的职责是协调空间，而不是充当一条具有特权的“主灯”。S3 直连灯带因此通过与 C3 相同的灯光节点运行时消费自己的最终目标状态，并可在产品不需要本地输出时关闭；这样新增语音、云端识别或其他控制器时，只需增加控制来源，不会改变场景所有权或节点模型。
