#include "SpatialLightPage.h"

String buildSpatialLightPage() {
  static const char page[] PROGMEM = R"HTML(
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="#edf7f6">
  <title>SOZO Flux 空间灯光</title>
  <style>
    :root{--ink:#253845;--muted:#8797a1;--line:#e1eaec;--paper:#fff;--soft:#f7faf9;--mint:#38ad9f;--mint-soft:#e9f8f5;--danger:#b86662}
    *{box-sizing:border-box}body{margin:0;min-width:320px;background:linear-gradient(145deg,#f7fbfb 0%,#eef6f5 55%,#f8fafb 100%);color:var(--ink);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",sans-serif}
    button,input,select{font:inherit}button{cursor:pointer}main{max-width:1240px;margin:0 auto;padding:26px 20px 42px}.page-intro{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;margin:4px 0 18px}.overline{color:#7ba9a3;font-size:10px;font-weight:850;letter-spacing:.13em}.page-intro h1{margin:6px 0 5px;font-size:28px;letter-spacing:-.045em}.page-intro p{max-width:620px;margin:0;color:var(--muted);font-size:13px;line-height:1.65}.header-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.connection{display:inline-flex;align-items:center;gap:6px;color:#70828d;font-size:11px;font-weight:750}.connection i{width:7px;height:7px;border-radius:50%;background:#c8d5d9}.connection.online i{background:#38ad9f;box-shadow:0 0 0 4px #dff4f0}.primary,.secondary,.danger{border:1px solid transparent;border-radius:10px;padding:9px 12px;font-size:11px;font-weight:800}.primary{color:#fff;background:#339e92}.secondary{color:#58707d;background:#fff;border-color:#dce7e9}.danger{color:#a95350;background:#fff7f6;border-color:#f0d9d7}.tabs{display:flex;gap:6px;margin:0 0 17px;padding:5px;border:1px solid var(--line);border-radius:13px;background:#fbfdfd}.tabs button{flex:1;border:0;border-radius:9px;padding:9px;color:#81909a;background:transparent;font-size:11px;font-weight:800}.tabs button.active{color:#167c72;background:#e8f7f4;box-shadow:0 1px 3px #b3dcd555}.view{display:none}.view.active{display:block}.card{min-width:0;padding:22px;border:1px solid var(--line);border-radius:18px;background:rgba(255,255,255,.92);box-shadow:0 13px 28px rgba(55,84,94,.055)}.card+.card{margin-top:17px}.card-heading{display:flex;align-items:flex-start;justify-content:space-between;gap:15px}.card-heading h2{margin:4px 0 0;color:#2b414d;font-size:16px;letter-spacing:-.025em}.card-heading p{margin:7px 0 0;color:#90a0a8;font-size:11px;line-height:1.55}.pill{display:inline-flex;align-items:center;gap:6px;white-space:nowrap;padding:6px 9px;border:1px solid #d7eae7;border-radius:999px;color:#3d9389;background:#f0faf8;font-size:10px;font-weight:800}.pill:before{content:"";width:6px;height:6px;border-radius:50%;background:#42b3a5}.pill.error{color:#ae706b;border-color:#eed8d6;background:#fff8f7}.pill.error:before{background:#cc7770}.status-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:17px}.metric{min-width:0;padding:11px;border-radius:11px;background:var(--soft)}.metric span{display:block;overflow:hidden;color:#9ba8af;font-size:9px;font-weight:800;white-space:nowrap;text-overflow:ellipsis}.metric strong{display:block;overflow:hidden;margin-top:5px;color:#304451;font-size:12px;white-space:nowrap;text-overflow:ellipsis}.library-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:19px}.effect{display:flex;align-items:center;gap:10px;min-width:0;border:1px solid #e4ebed;border-radius:13px;padding:12px;background:#fcfdfd;text-align:left;transition:.18s}.effect:hover{border-color:#b9dfda;transform:translateY(-1px)}.effect.selected{border-color:#8ad0c7;background:#eef9f7;box-shadow:0 0 0 3px #e7f7f4}.swatch{display:grid;place-items:center;flex:0 0 29px;width:29px;height:29px;border-radius:9px;color:#fff;font-size:15px;box-shadow:0 4px 11px #8bb4bd55}.effect-copy{min-width:0;display:grid;gap:4px}.effect strong{overflow:hidden;color:#354a58;font-size:11px;white-space:nowrap;text-overflow:ellipsis}.effect small{display:block;overflow:hidden;color:#9aa7af;font-size:9px;font-weight:650;white-space:nowrap;text-overflow:ellipsis}.parameter-grid{display:grid;gap:9px;margin-top:19px}.param{min-width:0;display:grid;gap:10px;padding:13px;border:1px solid #e5ecee;border-radius:13px;background:#fcfdfd}.param:hover{border-color:#c7e5e1}.param-head{display:flex;align-items:center;justify-content:space-between;gap:7px}.param strong{color:#405360;font-size:11px}.param output{overflow:hidden;color:#263b48;font-size:10px;font-weight:850;white-space:nowrap;text-overflow:ellipsis}.param input[type=range]{width:100%;accent-color:#39ab9f}.param input[type=color]{width:100%;height:25px;padding:0;border:0;background:transparent}.param select{width:100%;padding:8px 9px;border:1px solid #dfe8ea;border-radius:9px;color:#405461;background:#fff;font-size:10px;font-weight:750;outline:none}.toggle{display:flex;align-items:center;justify-content:space-between;width:100%;border:0;padding:0;background:transparent;text-align:left}.track{position:relative;width:34px;height:19px;border-radius:999px;background:#dbe5e8}.track i{position:absolute;top:3px;left:3px;width:13px;height:13px;border-radius:50%;background:#fff;box-shadow:0 1px 4px #25404b33;transition:.18s}.toggle.on .track{background:#42b3a5}.toggle.on .track i{left:18px}.panel-note{display:flex;align-items:center;gap:7px;margin:17px 0 0;color:#91a0a8;font-size:10px;line-height:1.55;font-weight:650}.panel-note:before{content:"✦";color:#3fa99e}.layout-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:17px}.layout-preview{min-height:250px;display:grid;place-items:center;padding:20px;border-radius:15px;background:linear-gradient(145deg,#f6fbfa,#eef6f7)}.strip-map{width:min(100%,460px);display:grid;gap:13px}.strip-line{display:flex;min-height:56px;overflow:hidden;border:1px solid #dbe8e9;border-radius:13px;background:#fff;box-shadow:0 6px 12px #57808a0b}.zone{display:grid;place-items:center;min-width:25px;padding:6px;color:#52707b;font-size:10px;font-weight:850}.zone.left{background:#e6f4f2}.zone.center{background:#f5faf9}.zone.right{background:#e9f1fb}.center-dot{display:grid;place-items:center;width:20px;height:20px;margin:-3px auto 0;border:4px solid #fff;border-radius:50%;color:#fff;background:#3caaa0;box-shadow:0 1px 4px #2a82784a;font-size:8px}.map-caption{display:flex;justify-content:space-between;color:#8e9da5;font-size:10px;font-weight:700}.layout-controls{display:grid;gap:10px}.segmented{display:grid;grid-template-columns:1fr 1fr;gap:7px}.segmented button{border:1px solid #e1e9eb;border-radius:11px;padding:10px;color:#7b8b95;background:#fbfcfc;font-size:10px;font-weight:800}.segmented button.active{border-color:#9cd8d0;color:#167d73;background:#eefaf8}.field{display:grid;gap:8px;padding:12px;border:1px solid #e5ecee;border-radius:12px;background:#fcfdfd}.field span{display:flex;justify-content:space-between;gap:8px;color:#71828d;font-size:10px;font-weight:800}.field output{color:#314653}.field input[type=number]{width:100%;padding:9px 10px;border:1px solid #dfe7e9;border-radius:9px;color:#354b58;background:#fff;outline:none}.switch-row{display:flex;align-items:center;justify-content:space-between;padding:11px 12px;border:1px solid #e4ebed;border-radius:12px;color:#40535f;background:#fcfdfd;font-size:11px;font-weight:800}.switch-row input{width:18px;height:18px;accent-color:#3baa9e}.warning{display:flex;gap:8px;margin-top:14px;padding:11px 12px;border:1px solid #eedfcf;border-radius:11px;color:#9b7a58;background:#fffaf4;font-size:10px;line-height:1.6;font-weight:650}.warning:before{content:"!";display:grid;place-items:center;flex:0 0 15px;height:15px;border-radius:50%;color:#fff;background:#d5a063;font-weight:900}.device-grid{display:grid;grid-template-columns:1fr 1fr;gap:17px}.device-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:17px}.hidden{display:none!important}.message{min-height:18px;margin-top:13px;color:#438f86;font-size:11px;font-weight:750}.message.error{color:#b56863}@media(max-width:1180px){.library-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.layout-grid,.device-grid{grid-template-columns:1fr}}@media(max-width:860px){main{padding:18px 13px 32px}.page-intro{display:grid}.header-actions{justify-content:flex-start}.library-grid,.status-grid{grid-template-columns:1fr 1fr}.effect{padding:10px}.card{padding:17px}}@media(max-width:560px){.page-intro h1{font-size:24px}.tabs{position:sticky;top:7px;z-index:3;box-shadow:0 7px 18px #476d7022}.library-grid{gap:7px}.effect small{display:none}.swatch{flex-basis:27px;width:27px;height:27px}.segmented{grid-template-columns:1fr}.status-grid{gap:6px}.metric{padding:9px}.header-actions .secondary{padding:8px 9px}}
  </style>
  <style>
    .extension-node-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:10px;margin-top:18px}.extension-heading-actions{display:flex;align-items:center;gap:8px}.extension-heading-actions .secondary{padding:8px 12px}.extension-heading-actions button:disabled{cursor:not-allowed;opacity:.5}.extension-node{display:grid;gap:10px;min-width:0;border:1px solid #e0eaec;border-radius:14px;padding:14px;background:#fcfdfd;text-align:left;transition:.18s}.extension-node:hover:not(:disabled){border-color:#9ed8d1;transform:translateY(-1px)}.extension-node.selected{border-color:#55b9ad;background:#eefaf8;box-shadow:0 0 0 3px #e6f7f4}.extension-node:disabled{cursor:not-allowed;opacity:.58}.extension-node-head,.extension-node-meta,.extension-control-head,.extension-summary{display:flex;align-items:center;justify-content:space-between;gap:10px}.extension-node strong{overflow:hidden;color:#334956;font-size:12px;white-space:nowrap;text-overflow:ellipsis}.extension-node small{color:#8799a2;font-size:10px;font-weight:700}.extension-node-meta{color:#6d838d;font-size:10px;font-weight:800}.node-dot{width:7px;height:7px;border-radius:50%;background:#a9bbc0;box-shadow:0 0 0 4px #eef3f4}.node-dot.online{background:#3fb0a2;box-shadow:0 0 0 4px #e1f5f1}.node-mode{padding:4px 7px;border-radius:999px;color:#687f89;background:#f0f5f5;font-size:9px;font-weight:850}.node-mode.independent{color:#6172be;background:#eff0ff}.extension-control{margin-top:17px}.extension-control-layout{display:grid;grid-template-columns:minmax(220px,.8fr) minmax(0,1.2fr);gap:14px;margin-top:18px}.extension-mode-panel,.extension-scene-panel{min-width:0;padding:15px;border:1px solid #e4ecee;border-radius:14px;background:#fcfdfd}.extension-mode-panel{display:grid;align-content:start;gap:12px}.extension-mode-copy{margin:0;color:#84969f;font-size:11px;line-height:1.65}.extension-mode-switch{display:grid;grid-template-columns:1fr 1fr;gap:7px}.extension-mode-switch button{min-height:64px;border:1px solid #e0e9eb;border-radius:12px;padding:10px;color:#6f838d;background:#fff;font-size:11px;font-weight:850;text-align:left}.extension-mode-switch button.active{border-color:#8bd4cb;color:#167d73;background:#eaf8f5;box-shadow:inset 0 0 0 1px #d6f0ec}.extension-mode-switch button:disabled{opacity:.55;cursor:not-allowed}.extension-summary{align-items:flex-start;padding:12px;border-radius:11px;background:#f4f8f8}.extension-summary div{display:grid;gap:4px}.extension-summary span{color:#95a4aa;font-size:9px;font-weight:800}.extension-summary strong{color:#38515d;font-size:12px}.extension-scene-panel{display:grid;gap:13px}.extension-effect-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px}.extension-effect-grid button{min-width:0;border:1px solid #e3ebed;border-radius:10px;padding:8px;color:#5d747e;background:#fff;font-size:10px;font-weight:800;white-space:nowrap;text-overflow:ellipsis;overflow:hidden}.extension-effect-grid button.active{border-color:#89d2c9;color:#177d73;background:#ebf9f6}.extension-parameter-grid{margin-top:0}.extension-actions{display:flex;justify-content:space-between;align-items:center;gap:10px}.extension-actions small{color:#8a9aa1;font-size:10px;line-height:1.5}.extension-empty{padding:17px;border:1px dashed #d9e6e8;border-radius:13px;color:#84969f;background:#fbfdfd;font-size:11px;line-height:1.6}.extension-unavailable{padding:18px;color:#8799a2;font-size:11px;line-height:1.6;text-align:center}@media(max-width:860px){.extension-control-layout{grid-template-columns:1fr}.extension-effect-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:560px){.extension-heading-actions{align-items:flex-end;flex-direction:column}.extension-node-list{grid-template-columns:1fr}.extension-mode-switch{grid-template-columns:1fr}.extension-effect-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.extension-actions{align-items:flex-start;flex-direction:column}.extension-actions .primary{width:100%}}
  </style>
  <style>
    .extension-led-count,.extension-firmware{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-top:14px;padding:15px;border:1px solid #e4ecee;border-radius:14px;background:#fcfdfd}.extension-led-count h3,.extension-firmware h3{margin:4px 0 0;font-size:13px}.extension-led-count p,.extension-firmware p{max-width:620px;margin:7px 0 0;color:#84969f;font-size:10px;line-height:1.6}.extension-led-count-form,.extension-firmware-form{display:flex;align-items:center;gap:8px}.extension-led-count-form input{width:92px;padding:8px 9px;border:1px solid #dfe8ea;border-radius:9px;color:#405461;background:#fff}.extension-firmware-form input[type=file]{max-width:260px;color:#71858f;font-size:10px}.extension-firmware-status{display:grid;gap:7px;min-width:220px}.extension-firmware-status progress{width:100%;height:8px;accent-color:#38ad9f}.extension-firmware-status small{color:#758b95;font-size:10px;font-weight:750}.extension-firmware button:disabled,.extension-firmware input:disabled{cursor:not-allowed;opacity:.55}@media(max-width:720px){.extension-led-count,.extension-firmware{align-items:stretch;flex-direction:column}.extension-led-count-form,.extension-firmware-form{align-items:stretch;flex-direction:column}.extension-firmware-status{min-width:0}.extension-firmware-form input[type=file]{max-width:100%}}
  </style>
  <style>
    .color-wheel{position:relative;width:min(100%,164px);aspect-ratio:1;margin:4px auto 1px;border:1px solid #d6e4e6;border-radius:50%;overflow:hidden;isolation:isolate;background:radial-gradient(circle at center,#fff 0%,#fff 8%,transparent 69%),conic-gradient(from 90deg,#ff4f66,#ffb24f,#e9ff4f,#52e979,#4fdcff,#697bff,#c66dff,#ff65c5,#ff4f66);box-shadow:inset 0 0 0 1px #ffffffaa,0 8px 16px #5b7c851c;cursor:crosshair;touch-action:none;user-select:none}.color-wheel:after{content:"";position:absolute;inset:15%;z-index:-1;border:1px solid #ffffff88;border-radius:50%;box-shadow:0 0 0 1px #2d455012}.color-wheel__marker{position:absolute;z-index:1;width:21px;height:21px;margin:-10.5px 0 0 -10.5px;border:3px solid #fff;border-radius:50%;box-shadow:0 0 0 2px #2d455099,0 2px 7px #23394355;pointer-events:none}.color-wheel__caption{display:flex;align-items:center;justify-content:center;min-height:18px;color:#82929b;font-size:9px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.color-input{display:grid;gap:8px}.color-input-switch{display:grid;grid-template-columns:1fr 1fr;gap:4px;padding:3px;border:1px solid #e1e9eb;border-radius:9px;background:#f5f8f8}.color-input-switch button{border:0;border-radius:6px;padding:6px;color:#82929b;background:transparent;font-size:9px;font-weight:850}.color-input-switch button.active{color:#167d73;background:#fff;box-shadow:0 1px 4px #4b73751f}.color-input-body{display:grid;place-items:center;min-height:164px}.color-palette{display:grid;grid-template-columns:repeat(9,minmax(0,1fr));gap:5px;width:min(100%,210px);margin:auto}.color-swatch-button{position:relative;min-width:0;aspect-ratio:1;border:1px solid #dbe4e6;border-radius:4px;box-shadow:inset 0 0 0 1px #ffffff55}.color-swatch-button.selected{outline:2px solid #314c59;outline-offset:1px}.color-swatch-button.selected:after{content:"\2713";position:absolute;inset:0;display:grid;place-items:center;color:#fff;font-size:10px;font-weight:900;text-shadow:0 1px 3px #000}.param:has(.color-wheel),.param:has(.color-input){gap:8px}.param:has(.color-wheel) .param-head,.param:has(.color-input) .param-head{padding-bottom:1px}@media(max-width:560px){.color-wheel{width:min(100%,188px)}.color-palette{gap:4px}.color-swatch-button{border-radius:3px}}
  </style>
  <style>
    .parameter-grid{display:grid;grid-template-columns:minmax(250px,1fr) minmax(0,2fr);grid-auto-rows:minmax(min-content,auto);align-items:stretch;gap:16px 20px;margin-top:24px}
    .parameter-item{display:grid;align-content:stretch;min-width:0;min-height:0}
    .parameter-item--color{grid-column:1;grid-row:span 2}
    .parameter-item--control{grid-column:2}
    .parameter-item--control-only,.parameter-item--no-color{grid-column:1/-1}
    .param-color{grid-column:auto;aspect-ratio:auto;min-height:0;padding:16px;background:linear-gradient(145deg,#f4fbfa,#fbfdfd)}
    .param-color .color-wheel{width:min(100%,164px)}
    .param-color--single{min-height:0}
    .param-long{grid-column:auto;min-height:0;height:100%;padding:16px}
    .param-long.param-brightness{min-height:0}
    .param-long.param-range{gap:9px}
    .param-long.param-choice,.param-long.param-toggle{min-height:0}
    .range-scale{display:flex;justify-content:space-between;color:#9aa8af;font-size:9px;font-weight:700}
    .param output{padding:4px 7px;border-radius:999px;background:#eef8f6}
    .param-color output{color:#237f75;background:#e6f6f2}
    .parameter-grid--no-color{grid-template-columns:minmax(0,1fr)}
    @media(max-width:860px){.parameter-grid{grid-template-columns:1fr;gap:16px}.parameter-item--color,.parameter-item--control,.parameter-item--control-only,.parameter-item--no-color{grid-column:1;grid-row:auto}.param-color,.param-long,.param-long.param-brightness{grid-column:auto}.param-long{height:auto}}
    @media(max-width:560px){.parameter-grid{gap:9px}}
  </style>
  <style>
    .extension-led-count{display:grid;grid-template-columns:minmax(0,1fr) minmax(190px,300px);align-items:center;gap:16px;margin-top:14px;padding:15px;border:1px solid #dcebea;border-radius:14px;background:linear-gradient(135deg,#f6fcfb,#fbfdfd)}.extension-led-count h3{margin:4px 0 0;color:#344b57;font-size:13px}.extension-led-count p{margin:6px 0 0;color:#82949d;font-size:10px;line-height:1.55}.extension-led-count-form{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px}.extension-led-count input{min-width:0;width:100%;padding:10px 11px;border:1px solid #cfe1e2;border-radius:10px;color:#314953;background:#fff;font-size:12px;font-weight:800;outline:none}.extension-led-count input:focus{border-color:#58bdb1;box-shadow:0 0 0 3px #dff4f1}.extension-led-count input:disabled{color:#9aa8ae;background:#f4f7f7}.extension-led-count button:disabled{cursor:not-allowed;opacity:.55}@media(max-width:560px){.extension-led-count{grid-template-columns:1fr}.extension-led-count-form{grid-template-columns:1fr}.extension-led-count-form .primary{width:100%}}
  </style>
  <style>
    .scope-indicator{display:grid;gap:2px;min-width:150px;padding:8px 11px;border:1px solid #d7e8e6;border-radius:11px;background:#f5fbfa}.scope-indicator span{color:#8b9ba2;font-size:8px;font-weight:850;letter-spacing:.08em}.scope-indicator strong{color:#277f76;font-size:11px}.pill.pending{color:#9b7a58;border-color:#eadbc9;background:#fffaf3}.pill.pending:before{background:#d3a064}.space-copy{max-width:720px}.node-scope-note{display:flex;align-items:center;gap:7px;margin-top:13px;color:#80929b;font-size:10px;line-height:1.55}.node-scope-note:before{content:"";width:6px;height:6px;border-radius:50%;background:#45afa3}.local-node-grid{display:grid;grid-template-columns:minmax(0,1.08fr) minmax(280px,.92fr);gap:14px;margin-top:18px}.local-node-panel{min-width:0;padding:16px;border:1px solid #e3ecee;border-radius:14px;background:#fcfdfd}.local-node-panel h3{margin:4px 0 0;color:#344b57;font-size:13px}.local-node-panel p{margin:7px 0 0;color:#84969f;font-size:10px;line-height:1.6}.local-node-panel .layout-preview{min-height:190px;margin-top:14px}.local-node-panel .layout-controls{margin-top:14px}.local-node-panel .warning{margin-top:12px}.system-grid{display:grid;grid-template-columns:minmax(0,1.1fr) minmax(300px,.9fr);gap:17px}.system-grid .card+.card{margin-top:0}.system-role-list{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:17px}.system-role{padding:12px;border:1px solid #e4ecee;border-radius:12px;background:#fbfdfd}.system-role span{display:block;color:#93a1a8;font-size:9px;font-weight:850}.system-role strong{display:block;margin-top:5px;color:#3a515d;font-size:11px}.system-copy{margin:13px 0 0;color:#8799a2;font-size:10px;line-height:1.65}.system-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:17px}@media(max-width:900px){.local-node-grid,.system-grid{grid-template-columns:1fr}.system-role-list{grid-template-columns:1fr 1fr}}@media(max-width:560px){.scope-indicator{min-width:0}.header-actions{align-items:stretch}.system-role-list{grid-template-columns:1fr}.local-node-panel{padding:13px}}
  </style>
  <style>
    .node-name-editor{display:grid;grid-template-columns:minmax(0,1fr) minmax(220px,360px);align-items:center;gap:16px;margin-top:16px;padding:14px;border:1px solid #dcebea;border-radius:14px;background:#f7fbfb}.node-name-editor h3{margin:4px 0 0;color:#344b57;font-size:13px}.node-name-editor p{margin:6px 0 0;color:#82949d;font-size:10px;line-height:1.55}.node-name-form{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px}.node-name-form input{min-width:0;width:100%;padding:10px 11px;border:1px solid #cfe1e2;border-radius:10px;color:#314953;background:#fff;font-size:12px;font-weight:750;outline:none}.node-name-form input:focus{border-color:#58bdb1;box-shadow:0 0 0 3px #dff4f1}.node-name-form input:disabled,.node-name-form button:disabled{cursor:not-allowed;opacity:.55}@media(max-width:680px){.node-name-editor{grid-template-columns:1fr}.node-name-form{grid-template-columns:1fr}.node-name-form .primary{width:100%}}
  </style>
</head>
<body>
<main>
  <header class="page-intro">
    <div><div class="overline">SOZO FLUX · SPATIAL CONTROL</div><h1>空间控制台</h1><p id="pageDescription" class="space-copy">设置当前空间场景；所有处于“跟随空间”的灯光节点会采用这里的效果和参数。</p></div>
    <div class="header-actions"><span id="scopeIndicator" class="scope-indicator"><span>作用范围</span><strong id="scopeValue">整个空间</strong></span><span id="connection" class="connection"><i></i><span id="connectionText">正在连接 Flux Hub</span></span><button id="spaceOffButton" class="secondary" onclick="turnOff()">关闭空间场景</button></div>
  </header>
  <nav class="tabs" aria-label="控制范围"><button class="active" data-tab="space">空间</button><button data-tab="nodes">灯光节点</button><button data-tab="system">中枢系统</button></nav>
  <section id="space" class="view active">
    <article class="card"><div class="card-heading"><div><div class="overline">SPACE SCENE</div><h2>当前空间场景</h2><p>这里只维护空间级渲染意图，不修改任何节点自己的灯珠数量、方向或启动设置。</p></div><span id="syncState" class="pill">等待同步</span></div><div id="statusGrid" class="status-grid"></div></article>
    <article class="card"><div class="card-heading"><div><div class="overline">SCENE EFFECT</div><h2>选择场景效果</h2><p>当前版本提供即时场景控制；命名、保存和自动切换将在 Scene System 中加入。</p></div><span class="pill">11 种效果</span></div><div id="effectGrid" class="library-grid"></div></article>
    <article class="card"><div class="card-heading"><div><div class="overline">SCENE PARAMETERS</div><h2 id="parameterTitle">场景参数</h2><p id="parameterCopy">选择效果后显示对应参数。</p></div></div><div id="parameterGrid" class="parameter-grid"></div><div id="effectNote" class="panel-note"></div><div id="lightingMessage" class="message"></div></article>
  </section>
  <section id="nodes" class="view">
    <article class="card"><div class="card-heading"><div><div class="overline">LIGHT NODES</div><h2>灯光节点</h2><p>S3 直连灯带与无线 C3 平级展示。节点配置只作用于选中的设备。</p></div><span id="lightNodePill" class="pill">正在读取</span></div><div id="lightNodeList" class="extension-node-list"></div><div id="nodeMessage" class="message"></div></article>
    <article id="nodeControlPanel" class="card extension-control"><div class="extension-unavailable">正在读取灯光节点…</div></article>
  </section>
  <section id="system" class="view">
    <div class="system-grid">
      <article class="card"><div class="card-heading"><div><div class="overline">FLUX HUB</div><h2>中枢状态</h2><p>Flux Hub 负责空间场景、节点安全连接、网络、音频输入与更新编排。</p></div><span id="hubStatePill" class="pill">正在连接</span></div><div id="hubStatusGrid" class="status-grid"></div><div class="system-role-list"><div class="system-role"><span>SCENE</span><strong>空间场景协调</strong></div><div class="system-role"><span>NODE FLEET</span><strong>节点连接与安全</strong></div><div class="system-role"><span>CONTROL INPUT</span><strong>网页与未来控制器</strong></div></div></article>
      <article class="card"><div class="card-heading"><div><div class="overline">NETWORK & MAINTENANCE</div><h2>网络与维护</h2><p id="networkInfo">—</p></div></div><p class="system-copy">以下操作只作用于 Flux Hub，不会修改灯光节点自己的布局或场景。</p><div class="system-actions"><button id="addLightNode" class="primary" type="button" onclick="openNodePairing()">添加无线节点</button><button class="secondary" onclick="resetWiFi()">重新配置 Wi‑Fi</button><button class="danger" onclick="restartDevice()">重启 Flux Hub</button></div><div id="pairingState" class="node-scope-note">普通模式只重连已知节点；添加窗口开启后才会持续发现新节点。</div><div class="warning">重置 Wi‑Fi 后，中枢会回到 SOZO-FLUX-SETUP 配网热点。</div><div id="systemMessage" class="message"></div></article>
    </div>
  </section>
</main>
<script>
const EFFECTS=[
 {id:'SOLID',label:'常亮',description:'单色稳定照明',color:'#59b9d8',colorMode:'single',params:[]},
 {id:'RAINBOW',label:'彩虹',description:'柔和彩色循环',color:'#7d8fff',colorMode:'none',params:[['rainbowStyle','彩虹风格','choice',[['0','柔和'],['1','鲜明'],['2','霓虹']]],['flowSpeed','流动速度','range',1,100,1,'级']]},
 {id:'COMET',label:'流星',description:'移动拖尾光束',color:'#6a9cf2',colorMode:'single',params:[['style','流星风格','choice',[['0','常亮色'],['1','彩虹'],['2','火焰'],['3','冰蓝'],['4','紫色']]],['cometTail','尾迹长度','range',5,80,1,'颗'],['cometSpeed','流动速度','range',1,50,1,'级'],['cometDensity','流星密度','range',1,8,1,'颗'],['cometBackground','背景亮度','range',0,60,1,'级'],['effectFlags','轻微随机变化','toggle']]},
 {id:'AUDIO',label:'拾音灯',description:'响应 Flux Hub 麦克风声音',color:'#d76de0',colorMode:'single',mic:true,params:[['sensitivityX100','拾音灵敏度','range',10,500,10,'%'],['style','拾音风格','choice',[['0','彩虹能量'],['1','常亮色'],['2','火焰'],['3','海洋'],['4','霓虹紫']]],['audioColorGainX100','颜色增益','range',0,500,10,'%'],['audioHueDrive','颜色驱动','choice',[['0','声音能量'],['1','低频'],['2','中频'],['3','高频']]]]},
 {id:'BREATH',label:'呼吸',description:'柔和明暗呼吸',color:'#8c77e8',colorMode:'single',params:[['breathFloorPercent','呼吸下限','range',0,60,1,'%']]},
 {id:'AURORA',label:'桌面极光',description:'低饱和渐变流动',color:'#3278dc',colorMode:'single',params:[['flowSpeed','流动速度','range',1,100,1,'级']]},
 {id:'FLAME_AUDIO',label:'火焰拾音',description:'声音驱动暖色火焰',color:'#f07d50',colorMode:'single',mic:true,params:[['sensitivityX100','拾音灵敏度','range',10,500,10,'%']]},
 {id:'GLASS_FLOW',label:'玻璃流光',description:'透明高光缓慢流动',color:'#e773a4',colorMode:'multiple',params:[['flowSpeed','流动速度','range',1,100,1,'级'],['secondaryColor','副色','color']]},
 {id:'CORNER_PULSE',label:'四角脉冲',description:'两端轻柔呼吸',color:'#3278dc',colorMode:'single',params:[['pulseAmplitudePercent','亮度起伏','range',0,100,1,'%'],['pulseHeightPercent','脉冲高度','range',0,100,1,'%']]},
 {id:'BASS_RIPPLE',label:'低频水波',description:'鼓点向两端扩散',color:'#8e7cff',colorMode:'single',mic:true,params:[['sensitivityX100','拾音灵敏度','range',10,500,10,'%'],['animationBrightness','动态亮度','range',0,255,1,'']]},
 {id:'FOCUS',label:'专注',description:'低亮稳定氛围光',color:'#f0a85a',colorMode:'single',params:[]}
];
const LOCAL_NODE_ID='local-s3';
let activeView='space',state=null,selected='SOLID',lightingOff=false,syncTimer=0,layoutProfile='continuous',nodeState=null,selectedNodeId=LOCAL_NODE_ID,nodePending=false,nodeNameBusy=false,nodeSyncTimer=0,nodeFirmwareState=null,nodeFirmwareBusy=false,nodeFirmwarePollTimer=0,nodeControlFocused=false;
const nodeDrafts={};
function createFormDrafts(){const values=Object.create(null),has=key=>Object.prototype.hasOwnProperty.call(values,key);return Object.freeze({read:(key,fallback)=>has(key)?values[key]:fallback,write:(key,value)=>(values[key]=value),clear:key=>delete values[key],hasPrefix:prefix=>Object.keys(values).some(key=>key.startsWith(prefix))})}
const formDrafts=createFormDrafts(),nodeFieldKey=(nodeId,field)=>`${nodeId}:${field}`,LOCAL_LAYOUT_DRAFT_KEY=nodeFieldKey(LOCAL_NODE_ID,'layout');
const $=id=>document.getElementById(id);
const nodeControlPanel=$('nodeControlPanel');
nodeControlPanel.onfocusin=()=>nodeControlFocused=true;
nodeControlPanel.onfocusout=event=>nodeControlFocused=!!(event.relatedTarget&&nodeControlPanel.contains(event.relatedTarget));
function setMessage(id,text,error=false){const el=$(id);if(!el)return;el.textContent=text||'';el.classList.toggle('error',!!error)}
function effectFor(id){return EFFECTS.find(x=>x.id===id)||EFFECTS[0]}
async function request(url,options){const response=await fetch(url,options);const data=await response.json().catch(()=>({ok:false,error:'设备返回无效数据'}));if(!response.ok||data.ok===false)throw new Error(data.error||'同步失败');return data}
function post(url,data){return request(url,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(data)})}
function localLightAvailable(){return !!state&&state.localLightEnabled!==false}
function remoteLightNodes(){return (nodeState&&Array.isArray(nodeState.nodes)?nodeState.nodes:[]).filter(node=>node.lightCapable===true)}
function remoteNodeOnline(node){return !!node&&node.state==='ready'&&node.bound===true}
function selectedRemoteNode(){return remoteLightNodes().find(node=>node.id===selectedNodeId)||null}
function localNodeName(){return String(state&&state.localLightName||'').trim()||'本地灯光节点'}
function defaultRemoteNodeName(node){return node?`无线灯光节点 · ${node.id.toUpperCase()}`:'无线灯光节点'}
function remoteNodeName(node){return String(node&&node.name||'').trim()||defaultRemoteNodeName(node)}
function selectedNodeLabel(){if(selectedNodeId===LOCAL_NODE_ID&&localLightAvailable())return localNodeName();const node=selectedRemoteNode();return node?remoteNodeName(node):'灯光节点'}
function selectLightNode(nodeId){if(nodeId!==selectedNodeId)formDrafts.clear(nodeFieldKey(selectedNodeId,'firmware'));selectedNodeId=nodeId;renderLightNodes()}
function currentLayout(){return formDrafts.read(LOCAL_LAYOUT_DRAFT_KEY,state&&state.layout)}
function ensureLayoutDraft(){const existing=formDrafts.read(LOCAL_LAYOUT_DRAFT_KEY,null);if(existing)return existing;return formDrafts.write(LOCAL_LAYOUT_DRAFT_KEY,{...state.layout})}
function nodeNameEditorMarkup(){return '<section class="node-name-editor"><div><div class="overline">DISPLAY NAME</div><h3>节点名称</h3><p>名称保存在 Flux Hub；设备 ID、绑定和控制地址不会改变。留空保存可恢复默认名称。</p></div><div class="node-name-form"><input id="nodeNameInput" type="text" maxlength="32" autocomplete="off" aria-label="节点名称"><button id="saveNodeNameButton" class="primary" type="button">保存名称</button></div></section>'}
function configureNodeNameEditor(nodeId,customName,fallback){const input=$('nodeNameInput'),button=$('saveNodeNameButton'),key=nodeFieldKey(nodeId,'name');if(!input||!button)return;const saved=String(customName||'');input.placeholder=fallback;input.value=formDrafts.read(key,saved);input.disabled=nodeNameBusy;button.disabled=nodeNameBusy;input.oninput=()=>{const characters=Array.from(input.value);if(characters.length>16)input.value=characters.slice(0,16).join('');formDrafts.write(key,input.value)};button.onclick=()=>saveNodeName(nodeId)}
async function saveNodeName(nodeId){const input=$('nodeNameInput'),button=$('saveNodeNameButton'),key=nodeFieldKey(nodeId,'name');if(!input||nodeNameBusy)return;const name=input.value.trim(),length=Array.from(name).length;if(length>16){setMessage('nodeMessage','节点名称最多 16 个字符',true);return}formDrafts.write(key,name);nodeNameBusy=true;input.disabled=true;if(button)button.disabled=true;try{const result=await post('/api/node/name',{id:nodeId,name});if(nodeId===LOCAL_NODE_ID){state.localLightName=result.name||''}else{const node=(nodeState&&nodeState.nodes||[]).find(item=>item.id===nodeId);if(node)node.name=result.name||''}formDrafts.clear(key);setMessage('nodeMessage',result.usingDefault?'已恢复默认节点名称':'节点名称已保存')}catch(error){setMessage('nodeMessage',error.message,true)}finally{nodeNameBusy=false;renderLightNodes()}}
function updateScopeContext(){const descriptions={space:'设置当前空间场景；所有处于“跟随空间”的灯光节点会采用这里的效果和参数。',nodes:'只配置当前选中的灯光节点；节点几何、启动行为和独立场景不会改变空间场景。',system:'管理 Flux Hub 的网络、节点发现、运行状态与维护操作。'};const scopes={space:'整个空间',nodes:selectedNodeLabel(),system:'Flux Hub'};$('scopeValue').textContent=scopes[activeView]||scopes.space;$('pageDescription').textContent=descriptions[activeView]||descriptions.space}
function activateView(view){activeView=view;document.querySelectorAll('[data-tab]').forEach(button=>button.classList.toggle('active',button.dataset.tab===view));document.querySelectorAll('.view').forEach(section=>section.classList.toggle('active',section.id===view));updateScopeContext();if(view==='nodes'||view==='system')void loadNodes(true)}
function renderSpaceStatus(){if(!state)return;const hubOnline=state.wifiState==='connected';$('connection').classList.toggle('online',hubOnline);$('connectionText').textContent=hubOnline?(state.mdns||'Flux Hub 已连接'):'Flux Hub 网络未连接';const remotes=remoteLightNodes(),revision=Number(state.sceneRevision)||0,followers=remotes.filter(node=>node.bound===true&&Number(node.controlMode)!==1),appliedRemotes=followers.filter(node=>remoteNodeOnline(node)&&(!revision||Number(node.lastSceneRevision)>=revision)),localCount=localLightAvailable()?1:0,totalFollowers=followers.length+localCount,appliedFollowers=appliedRemotes.length+localCount,sync=$('syncState');sync.className='pill'+(appliedFollowers<totalFollowers?' pending':'');sync.textContent=totalFollowers?`${appliedFollowers} / ${totalFollowers} 跟随节点已应用`:'空间场景已保存';const sceneName=lightingOff?'已关闭':effectFor(selected).label,list=[['当前场景效果',sceneName],['空间亮度',state.settings.brightness],['跟随应用',totalFollowers?`${appliedFollowers} / ${totalFollowers}`:'没有跟随节点'],['空间音频输入',state.micAvailable?'Hub 麦克风可用':'未检测到']];$('statusGrid').replaceChildren(...list.map(([label,value])=>{const div=document.createElement('div');div.className='metric';div.innerHTML=`<span>${label}</span><strong>${value}</strong>`;return div}))}
function renderHub(){if(!state)return;const pill=$('hubStatePill'),online=state.wifiState==='connected';pill.className='pill'+(online?'':' error');pill.textContent=online?'Hub 在线':'网络未连接';const known=Number(nodeState&&nodeState.knownCount)||remoteLightNodes().length,connected=Number(nodeState&&nodeState.onlineCount)||remoteLightNodes().filter(remoteNodeOnline).length,list=[['Hub 固件',state.hubFirmware||'未知'],['网络地址',state.ip||'—'],['无线节点',`${connected} / ${known} 在线`],['可用内存',`${Math.round((Number(state.freeHeap)||0)/1024)} KB`]];$('hubStatusGrid').replaceChildren(...list.map(([label,value])=>{const div=document.createElement('div');div.className='metric';div.innerHTML=`<span>${label}</span><strong>${value}</strong>`;return div}));$('networkInfo').textContent=online?`已连接 ${state.ssid||'路由器'} · ${state.ip||''}`:'当前未连接路由器';const add=$('addLightNode'),pairing=!!(nodeState&&nodeState.pairingWindowOpen),remaining=Math.max(0,Math.ceil((Number(nodeState&&nodeState.pairingRemainingMs)||0)/1000));if(add){add.disabled=pairing||nodeFirmwareBusy;add.textContent=pairing?`添加中 ${remaining} 秒`:'添加无线节点'}$('pairingState').textContent=pairing?`添加窗口已开启，还剩 ${remaining} 秒；现在让新 C3 长按 BOOT 进入配对。`:'普通模式只重连已知节点；添加窗口开启后才会持续发现新节点。'}
function renderEffects(){const grid=$('effectGrid');grid.replaceChildren(...EFFECTS.map(effect=>{const button=document.createElement('button');button.className='effect'+(!lightingOff&&selected===effect.id?' selected':'');button.innerHTML=`<span class="swatch" style="background:${effect.color}">✦</span><span class="effect-copy"><strong>${effect.label}</strong><small>${effect.description}</small></span>`;button.onclick=()=>chooseEffect(effect.id);return button}))}
 function parameterLayoutClass(def){const [key,,kind]=def;if(kind==='color')return 'param param-color'+(def[3]?' param-color--single':'');if(key==='brightness')return 'param param-long param-brightness';if(kind==='range')return 'param param-long param-range';if(kind==='choice')return 'param param-long param-choice';if(kind==='toggle')return 'param param-long param-toggle';return 'param param-long'}
function control(def){const [key,label,kind,a,b,c,d]=def;const value=state.settings[key];const wrap=document.createElement('div');wrap.className=parameterLayoutClass(def);if(kind==='range'){wrap.innerHTML=`<span class="param-head"><strong>${label}</strong><output>${value}${d||''}</output></span><input type="range" min="${a}" max="${b}" step="${c}" value="${value}"><span class="range-scale"><span>${a}${d||''}</span><span>${b}${d||''}</span></span>`;const input=wrap.querySelector('input');input.oninput=()=>{wrap.querySelector('output').textContent=input.value+(d||'');state.settings[key]=+input.value;queueLighting(key,input.value)}}else if(kind==='choice'){wrap.innerHTML=`<span class="param-head"><strong>${label}</strong><output></output></span><select>${a.map(([v,t])=>`<option value="${v}">${t}</option>`).join('')}</select>`;const select=wrap.querySelector('select');select.value=String(value);const out=wrap.querySelector('output');const update=()=>{out.textContent=a.find(x=>x[0]===select.value)?.[1]||select.value};update();select.onchange=()=>{state.settings[key]=+select.value;update();queueLighting(key,select.value)}}else if(kind==='toggle'){const enabled=(+value&1)===1;wrap.innerHTML=`<button class="toggle ${enabled?'on':''}"><span class="param-head"><strong>${label}</strong><output>${enabled?'开启':'关闭'}</output></span><span class="track"><i></i></span></button>`;const button=wrap.querySelector('button');button.onclick=()=>{const on=!button.classList.contains('on');button.classList.toggle('on',on);button.querySelector('output').textContent=on?'开启':'关闭';state.settings[key]=on?1:0;queueLighting(key,on?1:0)}}else if(kind==='color'){const color=value||'#ffffff';wrap.innerHTML=`<span class="param-head"><strong>${label}</strong><output>${color.toUpperCase()}</output></span><input type="color" value="${color}">`;const input=wrap.querySelector('input');input.oninput=()=>{const next=input.value.toUpperCase();wrap.querySelector('output').textContent=next;state.settings[key]=next;queueLighting(key,next)}}return wrap}
 function parameterPlan(effectOrId){const effect=typeof effectOrId==='string'?effectFor(effectOrId):effectOrId;const colorMode=effect.colorMode||'single';const hasSecondary=effect.params.some(def=>def[2]==='color');const defs=[];if(colorMode!=='none')defs.push(['color','主色','color',colorMode==='single'&&!hasSecondary]);defs.push(['brightness','亮度','range',0,255,1,'']);return [...defs,...effect.params]}
 function parameterGridClass(effectOrId){const effect=typeof effectOrId==='string'?effectFor(effectOrId):effectOrId;return 'parameter-grid'+(effect.colorMode==='none'?' parameter-grid--no-color':'')}
function renderLayout(){if(!state||!$('layoutPill'))return;const layout=currentLayout();layoutProfile=layout.profile;$('layoutPill').textContent=`${layout.activeCount} / ${layout.maxLedCount} 颗`;$('continuousButton').classList.toggle('active',layoutProfile==='continuous');$('segmentedButton').classList.toggle('active',layoutProfile==='segmented');const reverse=$('reverseSwitch');reverse.checked=layout.reversed;reverse.onchange=()=>ensureLayoutDraft().reversed=reverse.checked;const fields=$('layoutFields');fields.replaceChildren();const add=(key,label,value,min,max)=>{const labelEl=document.createElement('label');labelEl.className='field';labelEl.innerHTML=`<span>${label}<output>${value}</output></span><input id="layout_${key}" type="number" min="${min}" max="${max}" value="${value}">`;const input=labelEl.querySelector('input');input.oninput=()=>{labelEl.querySelector('output').textContent=input.value;ensureLayoutDraft()[key]=Number(input.value)};fields.append(labelEl)};add('activeCount','节点总灯珠数',layout.activeCount,1,layout.maxLedCount);if(layoutProfile==='continuous'){add('centerIndex','节点中心（从 0 开始）',layout.centerIndex,0,Math.max(0,layout.activeCount-1))}else{add('leftCount','左侧数量',layout.leftCount,0,layout.activeCount);add('centerCount','中间数量',layout.centerCount,1,layout.activeCount);add('rightCount','右侧数量',layout.rightCount,0,layout.activeCount)}const left=layoutProfile==='segmented'?layout.leftCount:Math.max(1,layout.centerIndex);const center=layoutProfile==='segmented'?layout.centerCount:1;const right=layoutProfile==='segmented'?layout.rightCount:Math.max(1,layout.activeCount-layout.centerIndex-1);$('layoutPreview').innerHTML=`<div class="strip-map"><div class="strip-line"><div class="zone left" style="flex:${left}">左 ${left}</div><div class="zone center" style="flex:${center}">中 ${center}<i class="center-dot">●</i></div><div class="zone right" style="flex:${right}">右 ${right}</div></div><div class="map-caption"><span>${layoutProfile==='segmented'?'左／中／右节点分段':'连续节点灯带'}</span><span>${layout.reversed?'方向已反转':'默认方向'}</span></div></div>`}
function extensionDraft(node){if(!node||!state)return null;if(!nodeDrafts[node.id])nodeDrafts[node.id]={effect:selected,settings:{...state.settings}};return nodeDrafts[node.id]}
function extensionModeName(node){return Number(node&&node.controlMode)===1?'独立控制':'跟随空间'}
function remoteNodeStateName(node){if(remoteNodeOnline(node))return '在线且已绑定';if(node&&node.bound===true)return '已绑定 · 当前离线';return '等待绑定'}
function makeNodeCard(name,online,leftMeta,rightMeta,mode,independent,selected,onSelect){const button=document.createElement('button'),head=document.createElement('span'),title=document.createElement('strong'),dot=document.createElement('i'),meta=document.createElement('span'),left=document.createElement('span'),right=document.createElement('span'),modePill=document.createElement('span');button.className='extension-node'+(selected?' selected':'');head.className='extension-node-head';title.textContent=name;dot.className='node-dot'+(online?' online':'');head.append(title,dot);meta.className='extension-node-meta';left.textContent=leftMeta;right.textContent=rightMeta;meta.append(left,right);modePill.className='node-mode'+(independent?' independent':'');modePill.textContent=mode;button.append(head,meta,modePill);button.onclick=onSelect;return button}
function renderLightNodes(preserveActiveEditor=false){const list=$('lightNodeList'),pill=$('lightNodePill');if(!list||!pill)return;const previousSelectedNodeId=selectedNodeId,remotes=remoteLightNodes(),ids=[...(localLightAvailable()?[LOCAL_NODE_ID]:[]),...remotes.map(node=>node.id)];if(!ids.includes(selectedNodeId)){formDrafts.clear(nodeFieldKey(selectedNodeId,'firmware'));selectedNodeId=ids[0]||''}const localOnline=localLightAvailable()?1:0,remoteOnline=remotes.filter(remoteNodeOnline).length,total=ids.length,online=localOnline+remoteOnline;pill.className='pill'+(online?'':' error');pill.textContent=total?`${total} 个节点 · ${online} 个在线`:'没有灯光节点';const cards=[];if(localLightAvailable())cards.push(makeNodeCard(localNodeName(),true,'S3 直连 · 在线',`${state.layout.activeCount} 颗灯珠`,'跟随空间',false,selectedNodeId===LOCAL_NODE_ID,()=>selectLightNode(LOCAL_NODE_ID)));remotes.forEach(node=>{const online=remoteNodeOnline(node);cards.push(makeNodeCard(remoteNodeName(node),online,`ID ${node.id.toUpperCase()} · ${remoteNodeStateName(node)}`,`${node.ledCount||'—'} 颗灯珠`,extensionModeName(node),Number(node.controlMode)===1,node.id===selectedNodeId,()=>selectLightNode(node.id)))});if(cards.length)list.replaceChildren(...cards);else list.innerHTML='<div class="extension-empty">此 Hub 未启用本地灯光输出，也没有已知无线灯光节点。请到“中枢系统”开启添加窗口。</div>';const preserveControl=preserveActiveEditor&&previousSelectedNodeId===selectedNodeId&&(nodeControlFocused||formDrafts.hasPrefix(`${selectedNodeId}:`));if(!preserveControl)renderLightNodeControl();renderSpaceStatus();updateScopeContext()}
function extensionParameterControl(def,draft){const [key,label,kind,a,b,c,d]=def;const value=draft.settings[key];const wrap=document.createElement('div');wrap.className=parameterLayoutClass(def);if(kind==='range'){wrap.innerHTML=`<span class="param-head"><strong>${label}</strong><output>${value}${d||''}</output></span><input type="range" min="${a}" max="${b}" step="${c}" value="${value}"><span class="range-scale"><span>${a}${d||''}</span><span>${b}${d||''}</span></span>`;const input=wrap.querySelector('input');input.oninput=()=>{wrap.querySelector('output').textContent=input.value+(d||'');draft.settings[key]=+input.value}}else if(kind==='choice'){wrap.innerHTML=`<span class="param-head"><strong>${label}</strong><output></output></span><select>${a.map(([v,t])=>`<option value="${v}">${t}</option>`).join('')}</select>`;const select=wrap.querySelector('select'),out=wrap.querySelector('output');select.value=String(value);const update=()=>out.textContent=a.find(item=>item[0]===select.value)?.[1]||select.value;update();select.onchange=()=>{draft.settings[key]=+select.value;update()}}else if(kind==='toggle'){const enabled=(+value&1)===1;wrap.innerHTML=`<button class="toggle ${enabled?'on':''}"><span class="param-head"><strong>${label}</strong><output>${enabled?'开启':'关闭'}</output></span><span class="track"><i></i></span></button>`;const button=wrap.querySelector('button');button.onclick=()=>{const on=!button.classList.contains('on');button.classList.toggle('on',on);button.querySelector('output').textContent=on?'开启':'关闭';draft.settings[key]=on?1:0}}return wrap}
function extensionColorControl(def,draft,modeKey){const [key,label]=def,color=normalizeColorHex(draft.settings[key]),wrap=document.createElement('div');wrap.className=parameterLayoutClass(def);wrap.innerHTML='<span class="param-head"><strong>'+label+'</strong><output>'+color+'</output></span>';const output=wrap.querySelector('output');wrap.append(createColorChooser(color,label,modeKey,next=>{output.textContent=next;draft.settings[key]=next}));return wrap}
function renderExtensionSceneControls(panel,node){const draft=extensionDraft(node);if(!draft)return;const scene=document.createElement('div');scene.className='extension-scene-panel';scene.innerHTML='<div class="extension-control-head"><div><div class="overline">INDEPENDENT SCENE</div><h2>独立场景</h2></div><span class="pill">节点本地保存</span></div>';const effectGrid=document.createElement('div');effectGrid.className='extension-effect-grid';effectGrid.replaceChildren(...EFFECTS.map(effect=>{const button=document.createElement('button');button.className=draft.effect===effect.id?'active':'';button.textContent=effect.label;button.onclick=()=>{draft.effect=effect.id;renderLightNodeControl()};return button}));scene.append(effectGrid);const effect=effectFor(draft.effect),grid=document.createElement('div');grid.className=parameterGridClass(effect)+' extension-parameter-grid';const layout=parameterGridItems(effect);grid.replaceChildren(...layout.items.map(item=>{const itemEl=document.createElement('div');itemEl.className='parameter-item parameter-item--'+item.kind;itemEl.style.gridColumn=item.column;itemEl.style.gridRow=`${item.row} / span ${item.rowSpan}`;itemEl.append(item.def[2]==='color'?extensionColorControl(item.def,draft,`node-${node.id}-${item.def[0]}`):extensionParameterControl(item.def,draft));return itemEl}));scene.append(grid);const actions=document.createElement('div');actions.className='extension-actions';actions.innerHTML='<small>编辑中的参数不会影响空间场景；点击应用后才写入此节点。</small><button class="primary">应用到此节点</button>';actions.querySelector('button').onclick=()=>syncExtensionLighting();scene.append(actions);panel.append(scene)}
function renderLocalNodeSettings(){if(!state||!$('startupColor'))return;const colorKey=nodeFieldKey(LOCAL_NODE_ID,'startupColor'),speedKey=nodeFieldKey(LOCAL_NODE_ID,'startupSpeed'),color=$('startupColor'),speed=$('startupSpeed');color.value=formDrafts.read(colorKey,state.startupColor);speed.value=formDrafts.read(speedKey,String(state.startupSpeed));$('startupSpeedValue').textContent=`${Number(speed.value).toFixed(1)}`;color.oninput=()=>formDrafts.write(colorKey,color.value);speed.oninput=event=>{formDrafts.write(speedKey,event.target.value);$('startupSpeedValue').textContent=event.target.value}}
function renderLocalNodeControl(panel){panel.innerHTML=`<div class="card-heading"><div><div class="overline">LOCAL LIGHT NODE</div><h2 id="nodeControlName"></h2><p>S3 直连灯带只是空间场景的一个消费者；以下配置仅保存在这个节点。</p></div><span id="layoutPill" class="pill">${state.layout.activeCount} 颗灯珠</span></div>${nodeNameEditorMarkup()}<div class="node-scope-note">当前模式：跟随空间 · 本机连接 · 无需无线配对</div><div class="local-node-grid"><section class="local-node-panel"><div class="overline">LED GEOMETRY</div><h3>灯珠几何</h3><p>配置此节点自己的灯珠数量、逻辑中心、分段与数据方向。</p><div id="layoutPreview" class="layout-preview"></div><div class="warning">1024 颗是软件上限。增加灯珠前请确认电源、供电注入和线材规格。</div></section><section class="local-node-panel"><div class="overline">LAYOUT EDITOR</div><h3>编辑节点布局</h3><div class="layout-controls"><div class="segmented"><button id="continuousButton" onclick="setLayoutProfile('continuous')">连续灯带</button><button id="segmentedButton" onclick="setLayoutProfile('segmented')">左／中／右分段</button></div><div id="layoutFields"></div><label class="switch-row">数据方向反转 <input id="reverseSwitch" type="checkbox"></label><button class="primary" onclick="saveLayout()">保存节点布局</button></div><div id="layoutMessage" class="message"></div></section></div><div class="local-node-grid"><section class="local-node-panel"><div class="overline">STARTUP</div><h3>开机动画</h3><p>只影响本地灯光节点上电时的颜色与展开速度。</p><div class="layout-controls"><label class="field"><span>开机颜色</span><input id="startupColor" type="color"></label><label class="field"><span>展开速度 <output id="startupSpeedValue"></output></span><input id="startupSpeed" type="range" min="0.1" max="5" step="0.1"></label><button class="primary" onclick="saveStartup()">保存开机动画</button></div></section><section class="local-node-panel"><div class="overline">CONTROL MODE</div><h3>跟随空间</h3><p>本地节点采用和 C3 相同的 LightNodeRuntime。当前版本固定跟随权威空间场景，节点几何不会被场景覆盖。</p><div class="extension-summary"><div><span>空间当前效果</span><strong>${lightingOff?'已关闭':effectFor(selected).label}</strong></div><div><span>空间亮度</span><strong>${state.settings.brightness}</strong></div></div></section></div>`;$('nodeControlName').textContent=localNodeName();configureNodeNameEditor(LOCAL_NODE_ID,state.localLightName,localNodeName());renderLayout();renderLocalNodeSettings()}
function renderRemoteNodeControl(panel,node){const independent=Number(node.controlMode)===1,online=remoteNodeOnline(node),header=`<div class="card-heading"><div><div class="overline">WIRELESS LIGHT NODE</div><h2 id="nodeControlName"></h2><p>设备 ID ${node.id.toUpperCase()} · 节点状态由 C3 回执确认。</p></div><span class="pill${online?'':' error'}">${remoteNodeStateName(node)}</span></div>`,nameEditor=nodeNameEditorMarkup();if(!online){const lastMode=extensionModeName(node);panel.innerHTML=header+nameEditor+`<div class="extension-unavailable"><strong>${lastMode} · ${node.ledCount||'—'} 颗灯珠</strong><br>灯光控制当前不可写，但名称仍可保存在 Hub；恢复加密连接后会保留最后已知配置。</div>`;$('nodeControlName').textContent=remoteNodeName(node);configureNodeNameEditor(node.id,node.name,defaultRemoteNodeName(node));return}panel.innerHTML=header+nameEditor+`<div class="extension-control-layout"><div class="extension-mode-panel"><div><div class="overline">CONTROL MODE</div><h2>${independent?'独立控制':'跟随空间'}</h2></div><p class="extension-mode-copy">${independent?'当前场景只在这个节点上运行。恢复跟随后会采用空间最新场景。':'当前节点采用空间场景的效果和参数，自己的灯珠数量保持不变。'}</p><div class="extension-mode-switch"><button data-mode="follow" class="${!independent?'active':''}" ${nodePending?'disabled':''}>跟随空间<br><small>采用权威空间场景</small></button><button data-mode="independent" class="${independent?'active':''}" ${nodePending?'disabled':''}>独立控制<br><small>保存节点场景</small></button></div><div class="extension-summary"><div><span>空间当前效果</span><strong>${lightingOff?'已关闭':effectFor(selected).label}</strong></div><div><span>空间亮度</span><strong>${state?state.settings.brightness:'—'}</strong></div></div></div></div>`;$('nodeControlName').textContent=remoteNodeName(node);configureNodeNameEditor(node.id,node.name,defaultRemoteNodeName(node));panel.querySelectorAll('[data-mode]').forEach(button=>button.onclick=()=>setExtensionControlMode(button.dataset.mode));const layout=panel.querySelector('.extension-control-layout');if(independent)renderExtensionSceneControls(layout,node);renderExtensionLedCountControl(panel,node);renderExtensionFirmwareControl(panel,node)}
function renderLightNodeControl(){const panel=$('nodeControlPanel');if(!panel)return;nodeControlFocused=false;if(selectedNodeId===LOCAL_NODE_ID&&localLightAvailable()){renderLocalNodeControl(panel);return}const node=selectedRemoteNode();if(node){renderRemoteNodeControl(panel,node);return}panel.innerHTML='<div class="extension-unavailable">选择一个灯光节点后查看配置。</div>'}
async function loadNodes(showError=false){try{nodeState=await request('/api/nodes',{cache:'no-store'});const firmware=await request('/api/node/firmware',{cache:'no-store'}),active=['starting','sending','verifying','restarting'].includes(firmware.state);nodeFirmwareState=firmware.state==='idle'?null:firmware;nodeFirmwareBusy=active;renderLightNodes(true);renderHub()}catch(error){if(showError)setMessage(activeView==='system'?'systemMessage':'nodeMessage',error.message,true)}}
async function openNodePairing(){const add=$('addLightNode');if(add)add.disabled=true;try{await post('/api/nodes/pairing',{});if(!nodeState)nodeState={nodes:[]};nodeState.pairingWindowOpen=true;nodeState.pairingRemainingMs=60000;setMessage('systemMessage','已开始 60 秒添加窗口，请让新 C3 长按 BOOT 进入配对');renderHub();await loadNodes(false)}catch(error){setMessage('systemMessage',error.message,true);if(add)add.disabled=false}}
const delay=milliseconds=>new Promise(resolve=>setTimeout(resolve,milliseconds));
async function waitForNode(nodeId,predicate){for(let attempt=0;attempt<7;attempt+=1){await delay(300);await loadNodes(false);const node=remoteLightNodes().find(item=>item.id===nodeId);if(node&&predicate(node))return node}return null}
async function setExtensionControlMode(mode){const node=selectedRemoteNode();if(!node||nodePending)return;nodePending=true;renderLightNodeControl();const beforeReceipt=Number(node.lastReceiptMs)||0;try{await post('/api/node/mode',{id:node.id,mode});const expected=mode==='independent'?1:0;const updated=await waitForNode(node.id,item=>(Number(item.lastReceiptMs)||0)>beforeReceipt);if(!updated)throw new Error('节点尚未确认模式切换，请稍后刷新');if(Number(updated.lastCommandError)!==0||Number(updated.controlMode)!==expected)throw new Error('节点拒绝了模式切换');setMessage('nodeMessage',mode==='independent'?'节点已确认独立控制':'节点已确认跟随空间')}catch(error){setMessage('nodeMessage',error.message,true)}finally{nodePending=false;renderLightNodes()}}
async function syncExtensionLighting(){const node=selectedRemoteNode(),draft=extensionDraft(node);if(!node||!draft||nodePending)return;nodePending=true;renderLightNodeControl();const beforeReceipt=Number(node.lastReceiptMs)||0;try{await post('/api/node/lighting',{id:node.id,effect:draft.effect,...draft.settings});const updated=await waitForNode(node.id,item=>(Number(item.lastReceiptMs)||0)>beforeReceipt);if(!updated)throw new Error('节点尚未确认独立场景，请稍后刷新');if(Number(updated.lastCommandError)!==0)throw new Error('节点拒绝了该场景');setMessage('nodeMessage','独立场景已由节点确认并保存')}catch(error){setMessage('nodeMessage',error.message,true)}finally{nodePending=false;renderLightNodes()}}
function render(preserveActiveEditor=false){renderSpaceStatus();renderEffects();renderParameters();renderHub();renderLightNodes(preserveActiveEditor);updateScopeContext()}
async function loadStatus(showError=true){try{state=await request('/api/status',{cache:'no-store'});lightingOff=state.effect==='OFF';if(!lightingOff&&state.effect)selected=state.effect;render(true);void loadNodes(false)}catch(error){if(showError){$('syncState').className='pill error';$('syncState').textContent='Hub 连接失败';setMessage('lightingMessage',error.message,true)}}}
function chooseEffect(id){lightingOff=false;selected=id;renderEffects();renderParameters();syncLighting({effect:id},true)}
function queueLighting(key,value){clearTimeout(syncTimer);syncTimer=setTimeout(()=>syncLighting({effect:selected,[key]:value}),160)}
async function syncLighting(data,immediate=false){if(!immediate&&syncTimer){clearTimeout(syncTimer);syncTimer=0}$('syncState').className='pill';$('syncState').textContent='正在保存空间场景';try{const response=await post('/api/lighting',data);if(response.settings)state.settings={...state.settings,...response.settings};if(response.effect)selected=response.effect;$('syncState').textContent='空间场景已保存';setMessage('lightingMessage','跟随空间的在线节点将采用最新场景')}catch(error){$('syncState').className='pill error';$('syncState').textContent='需要处理';setMessage('lightingMessage',error.message,true)}}
function setLayoutProfile(profile){const layout={...currentLayout(),profile};if(profile==='segmented'&&layoutProfile!=='segmented'){const n=layout.activeCount;layout.leftCount=Math.floor(n/4);layout.centerCount=n-Math.floor(n/4)*2;layout.rightCount=Math.floor(n/4)}formDrafts.write(LOCAL_LAYOUT_DRAFT_KEY,layout);layoutProfile=profile;setMessage('layoutMessage','节点布局草稿尚未保存');renderLayout()}
async function saveLayout(){const number=id=>Number($(id).value),maximum=Number(state.layout.maxLedCount)||1024;const data={profile:layoutProfile,activeCount:number('layout_activeCount'),reversed:$('reverseSwitch').checked?1:0};if(data.activeCount<1||data.activeCount>maximum){return setMessage('layoutMessage',`总灯珠数必须在 1–${maximum} 之间`,true)}if(layoutProfile==='continuous'){data.centerIndex=number('layout_centerIndex');if(data.centerIndex<0||data.centerIndex>=data.activeCount)return setMessage('layoutMessage','节点中心必须位于灯带范围内',true)}else{data.leftCount=number('layout_leftCount');data.centerCount=number('layout_centerCount');data.rightCount=number('layout_rightCount');if(data.leftCount+data.centerCount+data.rightCount!==data.activeCount)return setMessage('layoutMessage','左、中、右数量之和必须等于总灯珠数',true)}try{const result=await post('/api/layout',data);state.layout=result.layout;formDrafts.clear(LOCAL_LAYOUT_DRAFT_KEY);layoutProfile=result.layout.profile;setMessage('layoutMessage','本地节点布局已保存');renderLightNodes()}catch(error){setMessage('layoutMessage',error.message,true)}}
async function turnOff(){try{await request('/api/mode?value=off');lightingOff=true;renderEffects();renderSpaceStatus();setMessage('lightingMessage','空间场景已关闭；独立控制节点保持自己的场景');await loadStatus(false)}catch(error){setMessage('lightingMessage',error.message,true)}}
async function saveStartup(){const input=$('startupColor'),speed=$('startupSpeed');if(!input||!speed)return;const colorKey=nodeFieldKey(LOCAL_NODE_ID,'startupColor'),speedKey=nodeFieldKey(LOCAL_NODE_ID,'startupSpeed'),hex=input.value.slice(1),value=parseInt(hex,16);try{await request(`/api/startup?r=${value>>16&255}&g=${value>>8&255}&b=${value&255}&speed=${speed.value}`);state.startupColor=input.value;state.startupSpeed=Number(speed.value);formDrafts.clear(colorKey);formDrafts.clear(speedKey);setMessage('nodeMessage','本地节点开机动画已保存');renderLightNodes()}catch(error){setMessage('nodeMessage',error.message,true)}}
function restartDevice(){if(confirm('确定要重启 Flux Hub 吗？')){fetch('/api/restart').catch(()=>{});setMessage('systemMessage','Flux Hub 正在重启…')}}
function resetWiFi(){if(confirm('将清除 Flux Hub 已保存的 Wi‑Fi，并进入配置热点。继续吗？')){fetch('/api/wifi/reset').catch(()=>{});setMessage('systemMessage','正在进入 Wi‑Fi 配置模式…')}}
document.querySelectorAll('[data-tab]').forEach(button=>button.onclick=()=>activateView(button.dataset.tab));
function clamp(value,min,max){return Math.min(max,Math.max(min,value))}
function hsvToHex(h,s,v){const hue=((Number(h)%360)+360)%360;const saturation=clamp(Number(s)||0,0,1);const value=clamp(Number(v)||0,0,1);const chroma=value*saturation;const segment=hue/60;const x=chroma*(1-Math.abs(segment%2-1));let red=0,green=0,blue=0;if(segment<1){red=chroma;green=x}else if(segment<2){red=x;green=chroma}else if(segment<3){green=chroma;blue=x}else if(segment<4){green=x;blue=chroma}else if(segment<5){red=x;blue=chroma}else{red=chroma;blue=x}const offset=value-chroma;const hex=channel=>Math.round((channel+offset)*255).toString(16).padStart(2,'0');return `#${hex(red)}${hex(green)}${hex(blue)}`.toUpperCase()}
function hexToHsv(hex){const match=/^#?([0-9a-f]{6})$/i.exec(String(hex||''));if(!match)return {h:0,s:0,v:1};const value=parseInt(match[1],16);const red=(value>>16&255)/255,green=(value>>8&255)/255,blue=(value&255)/255;const max=Math.max(red,green,blue),min=Math.min(red,green,blue),delta=max-min;if(delta===0)return {h:0,s:0,v:max};let hue;if(max===red)hue=60*(((green-blue)/delta+6)%6);else if(max===green)hue=60*((blue-red)/delta+2);else hue=60*((red-green)/delta+4);return {h:hue,s:max===0?0:delta/max,v:max}}
const COLOR_PALETTE=[
  {name:'灰白',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#F7F8FA','#D8DEE3','#A5B0B8','#66747E','#26343D']},
  {name:'红色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#FFE3E6','#FF8A8A','#FF0000','#B80000','#660000']},
  {name:'橙色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#FFE8D8','#FFBD8A','#FF833D','#D8581E','#7A2D11']},
  {name:'黄色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#FFF4CC','#FFE07A','#F8BF32','#C98712','#704707']},
  {name:'绿色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#E0F7E8','#9BE3B3','#45C875','#218E4B','#10532E']},
  {name:'青色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#DCF8F7','#8CE4E1','#34C5C5','#168A91','#0D5059']},
  {name:'蓝色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#E1ECFF','#9DC0FF','#4B88F4','#2859B8','#17346C']},
  {name:'紫色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#EFE5FF','#C5A5F5','#8C62DB','#6337AA','#3A1F68']},
  {name:'粉色',levels:['浅色','柔和','标准','浓郁','深色'],colors:['#FFE2F1','#F9A1CF','#EB5AA6','#B72E78','#6D1948']}
];
const colorInputModes={};
const normalizeColorHex=value=>/^#[0-9a-f]{6}$/i.test(String(value||''))?String(value).toUpperCase():'#FFFFFF';
function createColorWheel(color,onChange){let hsv=hexToHsv(color);const wheel=document.createElement('div');wheel.className='color-wheel';wheel.setAttribute('role','slider');wheel.setAttribute('aria-label','颜色色轮');const marker=document.createElement('i');marker.className='color-wheel__marker';wheel.append(marker);const render=()=>{const radians=hsv.h*Math.PI/180;marker.style.left=`${50+Math.cos(radians)*hsv.s*50}%`;marker.style.top=`${50+Math.sin(radians)*hsv.s*50}%`;marker.style.background=hsvToHex(hsv.h,hsv.s,hsv.v);wheel.setAttribute('aria-valuetext',hsvToHex(hsv.h,hsv.s,hsv.v))};const update=event=>{const rect=wheel.getBoundingClientRect();const radius=Math.min(rect.width,rect.height)/2;if(!radius)return;const dx=event.clientX-(rect.left+rect.width/2),dy=event.clientY-(rect.top+rect.height/2);hsv.h=(Math.atan2(dy,dx)*180/Math.PI+360)%360;hsv.s=clamp(Math.hypot(dx,dy)/radius,0,1);render();onChange(hsvToHex(hsv.h,hsv.s,hsv.v))};let dragging=false;wheel.addEventListener('pointerdown',event=>{dragging=true;if(wheel.setPointerCapture)wheel.setPointerCapture(event.pointerId);update(event)});wheel.addEventListener('pointermove',event=>{if(dragging)update(event)});wheel.addEventListener('pointerup',()=>dragging=false);wheel.addEventListener('pointercancel',()=>dragging=false);render();return wheel}
function createColorPalette(color,onChange){const current=normalizeColorHex(color),palette=document.createElement('div');palette.className='color-palette';palette.setAttribute('role','group');palette.setAttribute('aria-label','预设颜色');for(let row=0;row<5;row+=1){COLOR_PALETTE.forEach(column=>{const hex=column.colors[row],button=document.createElement('button'),selected=hex===current;button.type='button';button.value=hex;button.className='color-swatch-button'+(selected?' selected':'');button.style.background=hex;button.setAttribute('aria-label',`${column.name} · ${column.levels[row]} · ${hex}`);button.setAttribute('aria-pressed',selected?'true':'false');button.onclick=()=>onChange(hex);palette.append(button)})}return palette}
function createColorChooser(color,label,modeKey,onChange){let current=normalizeColorHex(color);const chooser=document.createElement('div'),modeSwitch=document.createElement('div'),body=document.createElement('div');chooser.className='color-input';modeSwitch.className='color-input-switch';modeSwitch.setAttribute('role','group');modeSwitch.setAttribute('aria-label',label+'选择方式');body.className='color-input-body';const paletteButton=document.createElement('button'),wheelButton=document.createElement('button');paletteButton.type=wheelButton.type='button';paletteButton.textContent='色块';wheelButton.textContent='色轮';modeSwitch.append(paletteButton,wheelButton);const render=()=>{const mode=colorInputModes[modeKey]||'palette';[[paletteButton,'palette'],[wheelButton,'wheel']].forEach(([button,value])=>{const active=mode===value;button.className=active?'active':'';button.setAttribute('aria-pressed',active?'true':'false')});if(mode==='palette'){body.replaceChildren(createColorPalette(current,next=>{current=normalizeColorHex(next);onChange(current);render()}))}else{body.replaceChildren(createColorWheel(current,next=>{current=normalizeColorHex(next);onChange(current)}))}};paletteButton.onclick=()=>{colorInputModes[modeKey]='palette';render()};wheelButton.onclick=()=>{colorInputModes[modeKey]='wheel';render()};chooser.append(modeSwitch,body);render();return chooser}
 function colorControl(def){const [key,label]=def;const color=normalizeColorHex(state.settings[key]),wrap=document.createElement('div');wrap.className=parameterLayoutClass(def);wrap.innerHTML='<span class="param-head"><strong>'+label+'</strong><output>'+color+'</output></span>';const output=wrap.querySelector('output');wrap.append(createColorChooser(color,label,`main-${key}`,next=>{output.textContent=next;state.settings[key]=next;queueLighting(key,next)}));return wrap}
function parameterColumns(effectOrId){const defs=parameterPlan(effectOrId);return {colorDefs:defs.filter(def=>def[2]==='color'),controlDefs:defs.filter(def=>def[2]!=='color')}}
function parameterGridItems(effectOrId){const columns=parameterColumns(effectOrId);const items=[];let row=1;let controlIndex=0;if(columns.colorDefs.length===0){columns.controlDefs.forEach(def=>items.push({def,column:'1 / -1',row,rowSpan:1,kind:'no-color'}));return {items,rowCount:columns.controlDefs.length}}columns.colorDefs.forEach(def=>{items.push({def,column:'1',row,rowSpan:2,kind:'color'});for(let offset=0;offset<2&&controlIndex<columns.controlDefs.length;offset+=1){items.push({def:columns.controlDefs[controlIndex++],column:'2',row:row+offset,rowSpan:1,kind:'control'})}row+=2});while(controlIndex<columns.controlDefs.length){items.push({def:columns.controlDefs[controlIndex++],column:'1 / -1',row:row++,rowSpan:1,kind:'control-only'})}return {items,rowCount:row-1}}
function renderParameters(){if(!state)return;const effect=effectFor(selected);$('parameterTitle').textContent=effect.label;$('parameterCopy').textContent=effect.description;const grid=$('parameterGrid');grid.className=parameterGridClass(effect);const layout=parameterGridItems(effect);grid.replaceChildren(...layout.items.map(item=>{const itemEl=document.createElement('div');itemEl.className='parameter-item parameter-item--'+item.kind;itemEl.style.gridColumn=item.column;itemEl.style.gridRow=`${item.row} / span ${item.rowSpan}`;itemEl.append(control(item.def));return itemEl}));$('effectNote').textContent=effect.mic?'输入源：Flux Hub 麦克风（INMP441）':'空间场景参数会自动保存。'}
const standardControl=control;
control=def=>def[2]==='color'?colorControl(def):standardControl(def);
function extensionLedCount(node){const value=Number(node&&node.ledCount);return Number.isInteger(value)&&value>=1&&value<=512?value:null}
function renderExtensionLedCountControl(panel,node){const count=extensionLedCount(node),key=nodeFieldKey(node.id,'ledCount'),section=document.createElement('section');section.className='extension-led-count';section.innerHTML=`<div><div class="overline">NODE OUTPUT</div><h3>灯珠数量</h3><p>${count===null?'正在读取 C3 返回的灯珠数量。':'只保存到这个 C3；空间场景和独立场景都不会改动它。'}</p></div><div class="extension-led-count-form"><input id="extensionLedCount" type="number" min="1" max="512" step="1" inputmode="numeric" aria-label="C3 灯珠数量"><button class="primary" type="button">保存数量</button></div>`;const input=section.querySelector('#extensionLedCount'),button=section.querySelector('button');input.value=formDrafts.read(key,count===null?'':String(count));input.disabled=nodePending||count===null;button.disabled=nodePending||count===null;input.oninput=()=>formDrafts.write(key,input.value);button.onclick=saveExtensionLedCount;panel.append(section)}
function extensionOtaCapable(node){return !!node&&node.otaCapable===true}
function firmwareStateLabel(value){return {starting:'正在准备',sending:'正在传输',verifying:'正在校验',restarting:'C3 正在重启',failed:'更新失败',idle:'等待固件'}[value]||'等待固件'}
function renderExtensionFirmwareControl(panel,node){const section=document.createElement('section'),capable=extensionOtaCapable(node),firmwareDraftKey=nodeFieldKey(node.id,'firmware'),status=nodeFirmwareState&&nodeFirmwareState.nodeId===node.id?nodeFirmwareState:null,active=status&&['starting','sending','verifying','restarting'].includes(status.state),progress=status?Number(status.progress)||0:0;section.className='extension-firmware';if(!capable){section.innerHTML=`<div><div class="overline">BLE FIRMWARE</div><h3>蓝牙固件更新</h3><p>当前 C3 固件还不支持蓝牙更新。首次需要通过 USB 烧录支持 OTA 的引导版本，之后即可在这里更新。</p></div><span class="pill error">需要首次 USB 烧录</span>`;panel.append(section);return}section.innerHTML=`<div><div class="overline">BLE FIRMWARE · ${node.firmware||'未知版本'}</div><h3>蓝牙固件更新</h3><p>选择 C3 的 firmware.bin。S3 会通过已绑定且加密的 BLE 连接发送；中断或校验失败会继续使用旧固件。</p></div><div class="extension-firmware-status"><div class="extension-firmware-form"><input id="extensionFirmwareFile" type="file" accept=".bin,application/octet-stream" aria-label="选择 C3 固件"><button class="primary" type="button">${active?'更新中':'开始更新'}</button></div><progress max="100" value="${progress}"></progress><small>${status?`${firmwareStateLabel(status.state)} · ${progress}%`:'仅支持 ESP32-C3 应用固件'}</small></div>`;const input=section.querySelector('#extensionFirmwareFile'),button=section.querySelector('button');input.disabled=nodeFirmwareBusy||active;button.disabled=nodeFirmwareBusy||active;input.onchange=()=>input.files&&input.files.length?formDrafts.write(firmwareDraftKey,true):formDrafts.clear(firmwareDraftKey);button.onclick=uploadExtensionFirmware;panel.append(section)}
async function saveExtensionLedCount(){const node=selectedRemoteNode(),input=$('extensionLedCount');if(!node||!input||nodePending)return;const key=nodeFieldKey(node.id,'ledCount'),ledCount=Number(input.value);if(!Number.isInteger(ledCount)||ledCount<1||ledCount>512){setMessage('nodeMessage','请输入 1 到 512 的整数',true);return}formDrafts.write(key,input.value);nodePending=true;renderLightNodeControl();const beforeReceipt=Number(node.lastReceiptMs)||0;try{await post('/api/node/led-count',{id:node.id,ledCount});const updated=await waitForNode(node.id,item=>(Number(item.lastReceiptMs)||0)>beforeReceipt);if(!updated)throw new Error('C3 尚未确认灯珠数量，请稍后刷新');if(Number(updated.lastCommandError)!==0||Number(updated.ledCount)!==ledCount)throw new Error('C3 拒绝了这个灯珠数量');formDrafts.clear(key);setMessage('nodeMessage','灯珠数量已保存到此 C3')}catch(error){setMessage('nodeMessage',error.message,true)}finally{nodePending=false;renderLightNodes()}}
async function uploadExtensionFirmware(){const node=selectedRemoteNode(),input=$('extensionFirmwareFile'),file=input&&input.files&&input.files[0];if(!node||!file||nodeFirmwareBusy){setMessage('nodeMessage','请先选择 C3 的 firmware.bin 文件',true);return}if(!file.name.toLowerCase().endsWith('.bin')){setMessage('nodeMessage','请选择 .bin 格式的 C3 固件',true);return}if(!confirm(`确定更新无线灯光节点 ${node.id.toUpperCase()} 吗？更新期间请保持设备供电。`))return;formDrafts.clear(nodeFieldKey(node.id,'firmware'));nodeFirmwareBusy=true;renderLightNodeControl();renderHub();try{const form=new FormData();form.append('firmware',file,file.name);const response=await fetch(`/api/node/firmware?id=${encodeURIComponent(node.id)}`,{method:'POST',body:form}),data=await response.json().catch(()=>({ok:false,error:'设备返回无效数据'}));if(!response.ok||data.ok===false)throw new Error(data.error||'固件上传失败');nodeFirmwareState={state:'starting',nodeId:node.id,progress:0,error:'none'};setMessage('nodeMessage','固件已交给 Flux Hub，正在通过加密 BLE 发送到 C3');pollExtensionFirmware(node.id)}catch(error){nodeFirmwareBusy=false;setMessage('nodeMessage',error.message,true);renderLightNodeControl();renderHub()}}
async function pollExtensionFirmware(nodeId){if(nodeFirmwarePollTimer){clearTimeout(nodeFirmwarePollTimer);nodeFirmwarePollTimer=0}try{nodeFirmwareState=await request('/api/node/firmware',{cache:'no-store'});renderLightNodeControl();if(nodeFirmwareState.state==='failed'){nodeFirmwareBusy=false;setMessage('nodeMessage',`更新失败：${nodeFirmwareState.error||'未知错误'}`,true);renderLightNodeControl();renderHub();return}if(nodeFirmwareState.state==='idle'){nodeFirmwareState=null;const reconnected=await waitForNode(nodeId,remoteNodeOnline);nodeFirmwareBusy=false;if(!reconnected)throw new Error('固件已发送，但节点尚未重新连接');setMessage('nodeMessage','C3 固件更新完成，节点已重新连接');renderLightNodes();renderHub();return}nodeFirmwarePollTimer=setTimeout(()=>pollExtensionFirmware(nodeId),500)}catch(error){nodeFirmwareBusy=false;setMessage('nodeMessage',error.message,true);renderLightNodeControl();renderHub()}}
loadStatus();setInterval(()=>loadStatus(false),5000);
</script>
</body></html>
)HTML";
  return FPSTR(page);
}
